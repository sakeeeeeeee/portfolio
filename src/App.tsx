import { useEffect, useState } from 'react'
import { ArrowUpRight, Check, ChevronDown, ChevronRight, CircleArrowUp, Github, Linkedin, Mail, Menu, Moon, Send, Sparkles, Sun, X } from 'lucide-react'
import { achievements, projects, skills, timeline, type Project } from './data/portfolio'

const placeholder = (value: string) => value.startsWith('[')

function Nav({ onContact, theme, onToggleTheme }: { onContact: () => void; theme: 'dark' | 'light'; onToggleTheme: () => void }) { const [open, setOpen] = useState(false); return <header className="nav"><a className="brand" href="#top" aria-label="Home">FA<span>.</span></a><button className="menu-btn" onClick={() => setOpen(!open)} aria-label="Toggle navigation">{open ? <X /> : <Menu />}</button><nav className={open ? 'nav-links open' : 'nav-links'}><a href="#about" onClick={() => setOpen(false)}>About</a><a href="#skills" onClick={() => setOpen(false)}>Skills</a><a href="#projects" onClick={() => setOpen(false)}>Projects</a><a href="#journey" onClick={() => setOpen(false)}>Journey</a><a href="#contact" onClick={() => setOpen(false)}>Contact</a><button className="theme-btn" onClick={onToggleTheme} aria-label={theme === 'dark' ? 'Switch to light mode' : 'Switch to dark mode'}>{theme === 'dark' ? <Sun size={16} /> : <Moon size={16} />}</button></nav><button className="nav-cta" onClick={onContact}>Let's talk <ArrowUpRight size={16} /></button></header> }

function SectionLabel({ number, children }: { number: string; children: string }) { return <div className="section-label"><span>{number}</span><i></i>{children}</div> }
function Hero() { return <section className="hero" id="top"><div className="hero-copy"><p className="kicker"><span className="status-dot"></span> Available for new opportunities</p><h1>Turning ideas into <em>useful</em> digital experiences.</h1><p className="hero-text">I'm <strong>Farhan Aziz Harahap</strong>, an informatics student and software developer who enjoys turning complex problems into simple, thoughtful products — from Laravel web apps to Python automation.</p><div className="hero-actions"><a className="button primary" href="#projects">View my projects <ArrowUpRight size={17} /></a><a className="button ghost" href="#contact">Contact me <Mail size={16} /></a></div><div className="socials"><a href="https://github.com/sakeeeeeeee" aria-label="GitHub"><Github size={17} /> GitHub</a><a href="https://www.linkedin.com/in/farhan-aziz-harahap-728b421a5/" aria-label="LinkedIn"><Linkedin size={17} /> LinkedIn</a></div></div><div className="hero-visual"><div className="orbit orbit-one"></div><div className="orbit orbit-two"></div><div className="code-window"><div className="window-top"><span></span><span></span><span></span><small>developer.ts</small></div><div className="code"><p><b>01</b> <i>const</i> <strong>developer</strong> = {`{`}</p><p><b>02</b> &nbsp;name: <mark>'Farhan Aziz Harahap'</mark>,</p><p><b>03</b> &nbsp;focus: <mark>'building useful things'</mark>,</p><p><b>04</b> &nbsp;curiosity: <mark>true</mark>,</p><p><b>05</b> &nbsp;learning: <mark>'always'</mark></p><p><b>06</b> {`}`}</p><p><b>07</b></p><p><b>08</b> <i>return</i> <strong>impact</strong>(idea)</p></div></div><div className="visual-caption"><Sparkles size={15} /> Curiosity-driven, detail-oriented</div></div><a className="scroll-cue" href="#about">Scroll to explore <ChevronDown size={16} /></a></section> }
function About() { return <section className="section about" id="about"><div><SectionLabel number="01">A little about me</SectionLabel><h2>Curious by nature.<br /><em>Builder</em> by choice.</h2></div><div className="about-body"><p className="lead">I'm an informatics student at Universitas Putera Batam passionate about technology, problem solving, and the craft of building software that makes a difference.</p><p>My journey started with a simple curiosity about how things work. Today, I build database-backed web applications with Laravel and automation tools with Python — and I care about doing it properly: tested, versioned, and continuously integrated.</p><p>Every project is a chance to learn something new, challenge my assumptions, and become a more thoughtful developer.</p><div className="stats"><div><strong>7+</strong><span>Projects built</span></div><div><strong>10+</strong><span>Technologies explored</span></div><div><strong>3+</strong><span>Years learning</span></div></div></div></section> }
function Skills() { return <section className="section skills-section" id="skills"><SectionLabel number="02">What I work with</SectionLabel><div className="section-intro"><h2>Tools for turning<br /><em>thoughts into things.</em></h2><p>My toolkit is always evolving. These are the technologies I'm currently learning and using to bring ideas to life.</p></div><div className="skill-grid">{skills.map((group, i) => <div className="skill-group" key={group.title}><div className="skill-num">0{i + 1}</div><h3>{group.title}</h3><div className="skill-list">{group.items.map(item => <span key={item}><Check size={14} />{item}</span>)}</div></div>)}</div></section> }
function ProjectCard({ project, onOpen }: { project: Project; onOpen: (p: Project) => void }) { return <article className={project.featured ? 'project-card featured' : 'project-card'} onClick={() => onOpen(project)}><div className="project-image" style={{ backgroundImage: `linear-gradient(135deg, ${project.accent}22, var(--image-veil)), url(${project.image})` }}><span className="project-category">{project.category}</span><span className="view-project">View case study <ArrowUpRight size={18} /></span></div><div className="project-meta"><div><p className="eyebrow">{project.eyebrow}</p><h3>{project.title}</h3><p>{project.description}</p></div><span className="project-year">{project.year}</span></div><div className="tags">{project.tags.map(tag => <span key={tag}>{tag}</span>)}</div></article> }
function Projects({ onOpen }: { onOpen: (p: Project) => void }) { const [filter, setFilter] = useState('All'); const shown = filter === 'All' ? projects : projects.filter(p => p.category === filter); return <section className="section projects-section" id="projects"><div className="projects-head"><div><SectionLabel number="03">Selected work</SectionLabel><h2>Things I've <em>made.</em></h2></div><p>A selection of projects, experiments, and ideas that reflect how I think and build.</p></div><div className="filters">{['All', 'Web', 'Mobile', 'AI', 'Research'].map(item => <button className={filter === item ? 'active' : ''} onClick={() => setFilter(item)} key={item}>{item}</button>)}</div><div className="project-grid">{shown.map(project => <ProjectCard project={project} onOpen={onOpen} key={project.id} />)}</div></section> }
function Journey() { return <section className="section journey" id="journey"><div className="journey-head"><SectionLabel number="04">The journey so far</SectionLabel><h2>Always learning.<br /><em>Always moving.</em></h2></div><div className="timeline">{timeline.map((item, i) => <div className="timeline-item" key={item.year + i}><div className="timeline-marker">{i === timeline.length - 1 ? <CircleArrowUp size={18} /> : <span>0{i + 1}</span>}</div><div className="timeline-content"><strong>{item.year}</strong><h3>{item.title}</h3><p>{item.body}</p></div></div>)}</div></section> }
function Achievements() { return <section className="section achievements"><SectionLabel number="05">Beyond the code</SectionLabel><div className="achievement-list">{achievements.map((item, i) => <div className="achievement" key={item.title}><span className="achievement-index">0{i + 1}</span><span className="achievement-type">{item.type}</span><div><h3>{item.title}</h3><p>{item.org} · {item.year}</p></div><p className="achievement-detail">{item.detail}</p><ChevronRight size={18} /></div>)}</div></section> }
function Resume() { return <section className="resume"><div><SectionLabel number="06">A snapshot</SectionLabel><h2>Ready to bring<br /><em>ideas to life.</em></h2></div><div><p>For a more complete overview of my education, experience, projects, and achievements, take a look at my resume.</p><a className="button light" href="/resume.pdf" download>Download resume <ArrowUpRight size={17} /></a></div></section> }
function Contact() { return <section className="section contact" id="contact"><div><SectionLabel number="07">Let's connect</SectionLabel><h2>Have an idea?<br /><em>Let's talk.</em></h2><p>Whether you want to collaborate, share an opportunity, or simply say hello, my inbox is open.</p><div className="contact-links"><a href="mailto:ggfarhan07@gmail.com"><Mail size={17} /> ggfarhan07@gmail.com</a><a href="https://www.linkedin.com/in/farhan-aziz-harahap-728b421a5/"><Linkedin size={17} /> LinkedIn</a><a href="https://github.com/sakeeeeeeee"><Github size={17} /> GitHub</a></div></div><div className="contact-cta"><p>Prefer email? Write to me directly — I usually reply within a day. For opportunities, include a short note about the role or project and I'll get back to you with my resume and details.</p><a className="button primary" href="mailto:ggfarhan07@gmail.com?subject=Hello%20Farhan">Email me directly <Send size={16} /></a></div></section> }
function DetailModal({ project, onClose }: { project: Project; onClose: () => void }) { return <div className="modal-backdrop" onClick={onClose}><div className="modal" onClick={e => e.stopPropagation()}><button className="modal-close" onClick={onClose} aria-label="Close project detail"><X /></button><div className="modal-hero" style={{ backgroundImage: `linear-gradient(90deg, var(--modal-hero), var(--modal-hero-end)), url(${project.image})` }}><span className="project-category">{project.category}</span><h2>{project.title}</h2><p>{project.description}</p></div><div className="modal-content"><div className="detail-block"><span>Project overview</span><p>{project.description}</p></div>{[['Problem', project.problem], ['Goal', project.goal], ['My role', project.role], ['Design / development process', project.process], ['Challenges', project.challenges], ['Solution', project.solution], ['Result', project.result], ['What I learned', project.learned], ['Future improvements', project.future]].map(([title, text]) => <div className="detail-block" key={title}><span>{title}</span><p>{text}</p></div>)}<div className="modal-shots">{project.screenshots.map(src => <img src={src} key={src} alt="Project screenshot placeholder" />)}</div></div></div></div> }
function Footer() { return <footer><a className="brand" href="#top">FA<span>.</span></a><p>Built with curiosity and a lot of debugging.</p><div><a href="https://github.com/sakeeeeeeee" aria-label="GitHub"><Github size={17} /></a><a href="https://www.linkedin.com/in/farhan-aziz-harahap-728b421a5/" aria-label="LinkedIn"><Linkedin size={17} /></a><a href="mailto:ggfarhan07@gmail.com" aria-label="Email"><Mail size={17} /></a></div><small>© 2026 Farhan Aziz Harahap. All rights reserved.</small></footer> }
export default function App() { const [selected, setSelected] = useState<Project | null>(null); const [theme, setTheme] = useState<'dark' | 'light'>(() => (localStorage.getItem('theme') === 'light' ? 'light' : 'dark')); useEffect(() => { document.documentElement.dataset.theme = theme; localStorage.setItem('theme', theme) }, [theme]); return <><Nav onContact={() => document.getElementById('contact')?.scrollIntoView({ behavior: 'smooth' })} theme={theme} onToggleTheme={() => setTheme(theme === 'dark' ? 'light' : 'dark')} /><main><Hero /><About /><Skills /><Projects onOpen={setSelected} /><Journey /><Achievements /><Resume /><Contact /></main><Footer />{selected && <DetailModal project={selected} onClose={() => setSelected(null)} />}</> }